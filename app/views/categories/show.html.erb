<%= render "shared/breadcrumbs", items: breadcrumb_items_for_category(@category) %>
<h1 class="text-2xl font-semibold mb-4"><%= @category.name %></h1>

<% if @products.any? %>
  <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
    <% @products.each do |product| %>
      <%= render "products/shared/card", product: product %>
    <% end %>
  </div>
<% else %>
  <p class="text-gray-600">No products in this category yet.</p>
<% end %>
