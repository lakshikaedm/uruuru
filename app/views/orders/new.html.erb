<div class="mx-auto max-w-2xl p-6">
  <% content_for :title, t("orders.new.title") %>

  <h1 class="text-2xl font-semibold mb-4"><%= t("orders.new.heading") %></h1>

  <%= render "form", order: @order %>
</div>
