<div class="mx-auto max-w-3xl p-6">
  <h1 class="text-2xl font-semibold mb-4">
    <%= t("orders.edit.heading") %>
    <span class="font-mono">#<%= @order.id %></span>
  </h1>

  <%= render "form", order: @order %>
</div>
