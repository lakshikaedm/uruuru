<div class="w-full max-w-6xl mx-auto px-4">
  <%= render "shared/breadcrumbs", items: breadcrumb_items_for(@product) %>
  <%= render "products/shared/detail", product: @product %>

  <div class="mt-6 flex flex-wrap gap-2">
    <%= link_to "Edit Product", edit_product_path(@product), class: "rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 font-medium" %>
  </div>
</div>
